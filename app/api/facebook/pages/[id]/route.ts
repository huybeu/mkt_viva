import { ApiError, handleError, ok } from "@/lib/api"; import { currentUser } from "@/lib/auth"; import { facebook } from "@/services/facebook.service";
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { try { const user = await currentUser(); if (!user) throw new ApiError(401, "UNAUTHORIZED", "Vui lòng đăng nhập"); await facebook.remove((await params).id, user); return ok({ deleted: true }); } catch (e) { return handleError(e); } }
