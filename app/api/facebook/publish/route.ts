import { handleError, ok } from "@/lib/api"; import { facebookPublishSchema } from "@/lib/validation"; import { facebook } from "@/services/facebook.service";
export async function POST(request: Request) { try { const input = facebookPublishSchema.parse(await request.json()); const ids = input.facebookPageIds ?? [input.facebookPageId!]; const results = await Promise.allSettled(ids.map((id) => facebook.publish(input.postId, id))); return ok({ results: results.map((r, i) => r.status === "fulfilled" ? { pageId: ids[i], success: true, ...r.value } : { pageId: ids[i], success: false, error: r.reason instanceof Error ? r.reason.message : "Không thể đăng" }) }); } catch (e) { return handleError(e); } }
