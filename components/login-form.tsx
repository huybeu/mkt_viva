"use client";
import { FormEvent, useState } from "react";
export function LoginForm() { const [error,setError]=useState(""); const [busy,setBusy]=useState(false); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:f.get("username"),password:f.get("password")})});if(r.ok) location.href="/"; else {const j=await r.json().catch(()=>({}));setError(j.error||"Đăng nhập thất bại");setBusy(false)}} return <main className="flex min-h-screen items-center justify-center bg-canvas px-4"><form onSubmit={submit} className="w-full max-w-sm space-y-5 rounded-2xl bg-white p-7 shadow-xl"><div><h1 className="text-2xl font-bold">Content Team</h1><p className="mt-1 text-sm text-muted">Đăng nhập hệ thống quản lý bài đăng</p></div><label className="block text-sm font-semibold">Tên đăng nhập<input name="username" required className="input mt-2" autoFocus /></label><label className="block text-sm font-semibold">Mật khẩu<input name="password" type="password" required className="input mt-2" /></label>{error&&<p className="text-sm text-rose-600">{error}</p>}<button disabled={busy} className="btn-primary w-full">{busy?"Đang đăng nhập…":"Đăng nhập"}</button></form></main> }
